- **Consumers that switch exhaustively on `StopReason`:** the public `StopReason` union gains a member, `pause_turn`, reported when a provider pauses a long-running turn and expects the caller to resume it (Anthropic's `pause_turn`). Previously such a turn was mapped to `end_turn` and looked finished. **Who needs to act:** any code with an exhaustive `switch` over `StopReason`, or a `Record<StopReason, …>` lookup — TypeScript now reports a missing case or missing key, and at runtime a paused turn arrives carrying a token that never appeared before. **Migration:** add a `case 'pause_turn':` that resumes the turn, or a `default:` that treats an unfamiliar stop as non-final; mapping it to a completion reproduces the bug this fixes. **Unchanged:** every other `StopReason` member and its value, ordinary completions still reporting `end_turn`, and non-exhaustive consumers — an `if (stopReason === 'tool_use')` chain compiles and behaves exactly as before.
- Verified against mock provider responses only: `pause_turn` is emitted by long-running server-side tool turns, which are not cheap to induce on demand, so the mapping and its stop-reason plumbing are exercised with a stub adapter.
